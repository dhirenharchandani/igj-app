// ─── Release flags ────────────────────────────────────────────────────────────
// Set BETA_MODE to true during beta testing — removes all day-count gates so
// testers can access Patterns, Weekly Reset, and recommendations immediately.
// Flip to false before App Store / production release.

export const BETA_MODE = true
